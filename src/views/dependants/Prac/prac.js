import { Box, Typography } from "@mui/material";

export const Prac = () => {
  let content = (
    <Box>
      <Typography>Hello, this is a new prac</Typography>
    </Box>
  );
  return content;
};
